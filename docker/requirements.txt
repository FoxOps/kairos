# Dependances Python pour Leviia Schedule (production)
# Ce fichier contient uniquement les librairies necessaires pour l'application

# Framework web
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
Werkzeug==3.1.8

# i18n (francais/anglais) - gettext + catalogues de traduction
Flask-Babel==4.0.0

# Base de donnees (SQLAlchemy/Flask-SQLAlchemy/Alembic - le driver
# specifique au moteur choisi via DATABASE_URL est ajoute plus bas :
# psycopg pour PostgreSQL, PyMySQL pour MySQL/MariaDB)
SQLAlchemy==2.0.51
Flask-Migrate==4.1.0
alembic==1.18.5

# Export ICS
icalendar==7.2.0
python-dateutil==2.9.0.post0

# Timezone data for zoneinfo (stdlib) - the Alpine/musl base image
# doesn't ship the IANA tz database itself, so this pure-Python package is
# required for ZoneInfo(...) to resolve real timezone names.
tzdata==2026.3

# Configuration .env
python-dotenv==1.2.2

# Securite
flask-limiter==4.1.1
flask-cors==6.0.5
flask-talisman==1.1.0
flask-compress==1.24
cryptography>=49.0.0

# WSGI Server pour la production
gunicorn==26.0.0

# Authentification OIDC/SSO (requis pour l'import dans app/auth/oidc_auth.py)
Authlib==1.7.2
requests==2.34.2

# Pour PostgreSQL (optionnel - sera utilise si DATABASE_URL est postgresql)
psycopg[binary]>=3.3.4

# Pour MySQL/MariaDB (optionnel - sera utilise si DATABASE_URL est mysql/mariadb)
# - driver 100% pur Python, aucune lib systeme requise (contrairement a
# mysqlclient qui necessite les headers MySQL/MariaDB au build) - voir
# CLAUDE.md "Configuration: two parallel systems" pour le detail du choix
PyMySQL==1.2.0

# Monitoring (optionnel - pour Prometheus)
prometheus-client==0.25.0
psutil==7.2.2

# Sauvegardes S3/S3-compatible (optionnel - requis si BACKUP_S3_ENABLED=true,
# voir scripts/backup_config.py/backup_database.py) - import differe/try-except
# dans backup_database.py, mais sans ce paquet BACKUP_S3_ENABLED=true echoue
# silencieusement avec "boto3 non disponible" plutot que de sauvegarder.
boto3>=1.43.0
botocore>=1.43.0

# Notifications externes (Slack/Discord/Telegram/webhooks generiques) via
# Apprise - voir app/services/apprise_notification_service.py
apprise==1.12.0

# API REST publique (comptes de service, /api/v1/*) - flask-smorest genere
# l'OpenAPI depuis les schemas marshmallow, voir app/api/
flask-smorest==0.47.0
marshmallow==4.3.0
