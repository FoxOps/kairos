# Dependances Python pour Leviia Schedule (production)
# Ce fichier contient uniquement les librairies necessaires pour l'application

# Framework web
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
Werkzeug==3.1.8

# Base de donnees SQLite
SQLAlchemy==2.0.51

# Export ICS
icalendar==7.2.0
python-dateutil==2.9.0.post0
pytz==2026.2

# Configuration .env
python-dotenv==1.2.2

# Securite
flask-limiter==4.1.1
flask-cors==6.0.5
flask-talisman==1.1.0
flask-compress==1.24
cryptography>=49.0.0

# WSGI Server pour la production
gunicorn==26.0.0

# Authentification OIDC/SSO (requis pour l'import dans app/auth/oidc_auth.py)
Authlib==1.7.2
requests==2.34.2

# Pour PostgreSQL (optionnel - sera utilise si DATABASE_URL est postgresql)
psycopg[binary]>=3.3.4

# Pour Redis (optionnel - sera utilise si CACHE_TYPE=RedisCache)
redis==5.0.1

# Monitoring (optionnel - pour Prometheus)
prometheus-client==0.20.0
psutil==6.0.0
