# Framework web
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
Werkzeug==3.1.8

# i18n (français/anglais) - gettext + catalogues de traduction. Réintroduit
# pytz en dépendance transitive (ce projet utilise zoneinfo, pas pytz, pour
# tout le reste) - inoffensif, Flask-Babel ne l'utilise qu'en interne pour
# son propre mécanisme de formatage, jamais appelé ici (to_viewer_timezone()/
# format_date_fr restent le seul code de fuseau horaire réellement utilisé).
Flask-Babel==4.0.0

# Base de données
SQLAlchemy==2.0.51
Flask-Migrate==4.1.0
alembic==1.18.5

# Export ICS
icalendar==7.2.0
python-dateutil==2.9.0.post0

# Timezone data for zoneinfo (stdlib) - the Docker base image (Alpine/musl)
# doesn't ship the IANA tz database itself, so this pure-Python package is
# required for ZoneInfo(...) to resolve real timezone names.
tzdata==2026.3

# Pour le développement (optionnel)
python-dotenv==1.2.2

# Tests
pytest==9.1.1
pytest-flask==1.3.0
pytest-cov==7.1.0

# Linting et formatage
ruff==0.15.21
mypy==2.3.0
black==26.5.1

# Sécurité
bandit==1.9.4
safety==3.8.1

# Rate limiting et CORS
flask-limiter==4.1.1
flask-cors==6.0.5

# Sécurité - versions minimales requises pour corriger les vulnérabilités
# cryptography>=46.0.7 corrige CVE-2026-34073, CVE-2026-39892, CVE-2026-26007
# pip>=26.1 corrige CVE-2026-1703, CVE-2026-3219, CVE-2026-6357
cryptography>=49.0.0
pip>=26.1.2

# Compression
flask-compress==1.24

# Pour PostgreSQL (optionnel)
psycopg[binary]>=3.3.4

# Pour MySQL/MariaDB (optionnel) - driver 100% pur Python, aucune lib
# systeme requise (contrairement a mysqlclient qui necessite les headers
# MySQL/MariaDB au build) - voir CLAUDE.md "Configuration: two parallel
# systems" pour le detail du choix
PyMySQL==1.2.0

# Authentification OIDC/SSO (optionnel - pour Keycloak, Okta, etc.)
# Authlib est la bibliothèque moderne pour OIDC, remplaçant flask-oidc
Authlib==1.7.2
requests==2.34.2
flask-talisman==1.1.0

# Monitoring (optionnel - pour Prometheus)
prometheus-client==0.25.0
psutil==7.2.2

# Notifications externes (Slack/Discord/Telegram/webhooks génériques) via
# Apprise - voir app/services/apprise_notification_service.py
apprise==1.12.0

# API REST publique (comptes de service, /api/v1/*) - flask-smorest génère
# l'OpenAPI depuis les schémas marshmallow, voir app/api/ et CLAUDE.md
# "API publique (flask-smorest)". apispec/webargs sont des transitives de
# flask-smorest, non listées séparément (même convention que les autres
# transitives de ce fichier).
flask-smorest==0.47.0
marshmallow==4.3.0
